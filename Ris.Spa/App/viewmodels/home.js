define(['services/logger'], function (logger) {
    var vm = {
        activate: activate,
    };

    return vm;

    //#region Internal Methods
    function activate() {
        return true;
    }
    //#endregion
});